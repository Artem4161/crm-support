const AdminService = require('./admin');
const TokenService = require('./refreshToken');
const TicketService = require('./ticket');
const CheckService = require('./privilege');

module.exports = {
  AdminService,
  TokenService,
  TicketService,
  CheckService,
};
