const Admin = require('./admin');
const Ticket = require('./ticket');
const Token = require('./ticket');
const User = require('./user');
const Role = require('./role');
const Message = require('./message');

module.exports = {
  Admin,
  Ticket,
  Token,
  User,
  Role,
  Message,
};
