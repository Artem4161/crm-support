const seedAdmin = require('./admin');
const seedTicket = require('./ticket');
const seedUser = require('./user');
const seedRole = require('./role');

module.exports = {
  seedAdmin,
  seedTicket,
  seedUser,
  seedRole,
};
